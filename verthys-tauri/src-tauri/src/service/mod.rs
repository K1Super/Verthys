/*
 * service/mod.rs — 服务层模块入口
 *
 *    "分层单向依赖与隔离规则"
 *
 * 服务层职责：
 *   - 提供无状态业务函数，接收上下文参数
 *   - 调用持久层/工具层完成具体操作
 *   - 捕获特定业务异常并转换为统一错误结构
 *   - 不处理日志，仅返回 Result
 *   - 不直接引用持久层具体实现类，通过依赖注入工厂获取
 *
 * 依赖方向：service → repository / util（单向，禁止反向）
 */
pub mod verthys_service;
