/*
 * middleware/mod.rs — 中间件与上下文模块入口
 *
 *    "标准化日志分层输出规范"
 *             "分层目录结构设计"
 *
 * 中间件层职责：
 *   - 持有进程级上下文容器（Context），挂载日志管道发送端、全局配置等横切关注点
 *   - 日志管道发送端通过构造函数参数逐层注入至控制器、服务、工具等所有分层
 *   - 发送端不得设计为全局静态变量
 */
pub mod context;
pub mod panic_hook;
